const ReactDOM = require('react-dom');
const React = require('react');
const {Component} = require('react');
const $ = require('jquery');

let LearnReact = React.createClass({

    render(){
        return (
               <div className = 'LearnReact'>
                   LearnReact
               </div>
        );
    }
});
export default LearnReact;
