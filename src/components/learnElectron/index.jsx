const ReactDOM = require('react-dom');
const React = require('react');
const {Component} = require('react');
const $ = require('jquery');

let LearnElectron = React.createClass({

    render(){
        return (
               <div className = 'LearnElectron'>
                   LearnElectron
               </div>
        );
    }
});
export default LearnElectron;
